(()=>{const $=s=>document.querySelector(s),api=async(action,body)=>{const r=await fetch('notifications-api.php?action='+action,{method:body?'POST':'GET',headers:body?{'Content-Type':'application/json'}:{},body:body?JSON.stringify(body):undefined}),j=await r.json();if(!r.ok)throw Error(j.error||'Erreur');return j};const b64=value=>{const padding='='.repeat((4-value.length%4)%4),base=(value+padding).replace(/-/g,'+').replace(/_/g,'/'),raw=atob(base);return Uint8Array.from([...raw].map(c=>c.charCodeAt(0)))};let reminders=[];async function load(){try{const data=await api('reminders');reminders=data.reminders||[];const badge=$('#notificationBadge');badge.textContent=data.unread;badge.hidden=!data.unread;render()}catch(e){$('#pushStatus').textContent=e.message}}function render(){const list=$('#notificationList');list.innerHTML=reminders.length?reminders.map(r=>`<article class="notice ${r.read_at?'':'unread'}" data-notice="${r.id}" data-task="${r.task_id}"><b>${r.title}</b><small>${r.body}</small></article>`).join(''):'<p class="empty">Aucun rappel pour le moment.</p>';list.querySelectorAll('[data-notice]').forEach(row=>row.onclick=async()=>{try{await api('read',{id:row.dataset.notice});row.classList.remove('unread');document.querySelector('#notificationsDialog').close();document.querySelector('#task-'+CSS.escape(row.dataset.task))?.scrollIntoView({behavior:'smooth',block:'center'});await load()}catch(e){$('#pushStatus').textContent=e.message}})}async function enable(){const status=$('#pushStatus');try{if(!('serviceWorker'in navigator)||!('PushManager'in window)||!('Notification'in window))throw Error('Les notifications système ne sont pas disponibles sur cet appareil.');if(Notification.permission==='denied')throw Error('Les notifications sont bloquées dans les réglages du navigateur.');const config=await api('config');if(!config.available)throw Error('Les clés Web Push ne sont pas encore configurées sur le serveur.');const permission=await Notification.requestPermission();if(permission!=='granted')throw Error('Autorisation non accordée.');const registration=await navigator.serviceWorker.ready;let subscription=await registration.pushManager.getSubscription();if(!subscription)subscription=await registration.pushManager.subscribe({userVisibleOnly:true,applicationServerKey:b64(config.publicKey)});const json=subscription.toJSON();await api('subscribe',{endpoint:subscription.endpoint,keys:json.keys,contentEncoding:'aes128gcm'});status.textContent='Notifications activées sur cet appareil.'}catch(e){status.textContent=e.message}}$('#openNotifications').onclick=()=>{$('#notificationsDialog').showModal();load()};$('#enablePush').onclick=enable;load()})();
